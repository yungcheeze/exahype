#include "EulerFlow3d/Constants.h"

Packed-Type: int;

Constant: EXAHYPE_NUMBER_OF_PDES;  // The number of PDEs we want to run on the same grid.

class exahype::dastgen::Cell {  
  parallelise persistent int cellDescriptionsIndex; //!< We specify a cellDescription for each PDE that should run on the grid.
};
